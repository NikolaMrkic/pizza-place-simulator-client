import { EditableColumn } from './Editable';
declare const useProps: <T extends object>(dataSource: T[], columns: EditableColumn<T>[], onCellChange: (nextSource: T[], curValue: any, beforeValue: any, rowIndex: number, dataIndex: string) => void, form: any) => {
    cacheSource: any[];
    editColumns: EditableColumn<T>[];
    hasError: boolean;
};
export default useProps;
